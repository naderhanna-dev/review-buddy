{
  "name": "reviewradar",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "serve": "pnpm --filter server run serve",
    "install-service": "pnpm --filter server run -- install-service",
    "uninstall-service": "pnpm --filter server run -- uninstall-service",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.12.1"
}
