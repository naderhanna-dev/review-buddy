{
  "name": "reviewradar",
  "private": true,
  "bin": {
    "reviewradar": "./bin/reviewradar.sh"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "serve": "pnpm --filter server run serve",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.12.1"
}
